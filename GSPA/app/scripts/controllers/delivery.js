'use strict';

/**
 * @ngdoc function
 * @name gspaApp.controller:DeliveryCtrl
 * @description
 * # DeliveryCtrl
 * Controller of the gspaApp
 */
angular.module('gspaApp')
  .controller('DeliveryCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
