'use strict';

/**
 * @ngdoc function
 * @name gspaApp.controller:ContactsCtrl
 * @description
 * # ContactsCtrl
 * Controller of the gspaApp
 */
angular.module('gspaApp')
  .controller('ContactsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
